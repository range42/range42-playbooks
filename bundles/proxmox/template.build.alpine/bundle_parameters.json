{
  "_generated": "DO NOT EDIT - generated from bundle_parameters.src.yml by _tools/generate-bundle-params.py",
  "tier": "proxmox",
  "bundle": "proxmox/template.build.alpine",
  "description": "Create the Alpine VM template family on a Proxmox hypervisor - clone-less inline build then convert to template. Idempotent, skips templates already finalized.",
  "optional": false,
  "params": [
    {
      "name": "templates_whitelist",
      "type": "list",
      "required": false,
      "default_where": "bundle-inline",
      "default": [],
      "items": "short code string, e.g. alpine-nano",
      "description": "If empty build the full alpine set, otherwise only the listed short codes."
    },
    {
      "name": "template_net_bridge",
      "type": "string",
      "required": false,
      "default_where": "bundle-inline",
      "default": "vmbr140",
      "description": "Bridge the template VM NIC attaches to while it is being built. Defaults to vmbr140, which is what every caller got when the value was hardcoded - so an existing caller that passes nothing is unaffected. A caller on SDN passes its vnet instead, for instance net140, which is a bridge name as far as qemu is concerned."
    }
  ]
}
