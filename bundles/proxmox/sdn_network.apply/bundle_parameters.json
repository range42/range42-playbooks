{
  "_generated": "DO NOT EDIT - generated from bundle_parameters.src.yml by _tools/generate-bundle-params.py",
  "tier": "proxmox",
  "bundle": "proxmox/sdn_network.apply",
  "description": "Flush the pending SDN config into the running one - PUT /cluster/sdn, then poll the UPID to completion. Preserve untouched NAT sources, with explicit review of existing source CIDRs whose rules may change. Apply remains cluster-wide; preservation currently covers one node.",
  "optional": false,
  "params": [
    {
      "name": "BUNDLE_SDN_CHANGED_SOURCES",
      "type": "list",
      "required": false,
      "default": [],
      "default_where": "bundle-inline",
      "description": "Up to 64 unique canonical IPv4 source CIDRs whose SNAT/MASQUERADE rules may be removed, reordered or replaced by pending configuration. The exact list is validated before apply and bound to its snapshot. Other existing sources and every non-NAT rule remain protected. For a CIDR change, list both the old and new CIDR. This does not scope the global apply."
    },
    {
      "name": "proxmox_node",
      "type": "string",
      "required": true,
      "from_vault": true,
      "default_where": "none",
      "description": "Proxmox node the apply request goes through, resolved from the scenario vault. The apply itself is cluster-wide - the node decides where the request lands, not what it reloads."
    }
  ]
}
