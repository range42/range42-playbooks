{
  "_generated": "DO NOT EDIT - generated from bundle_parameters.src.yml by _tools/generate-bundle-params.py",
  "tier": "proxmox",
  "bundle": "proxmox/sdn_network.bootstrap",
  "description": "Bring the declared SDN networks up on a Proxmox cluster. Verify complete cluster coverage before creating missing objects or updating explicit drift, apply at most once when needed, and reconcile SNAT on each zone's members while preserving unrelated rules.",
  "optional": false,
  "params": [
    {
      "name": "BUNDLE_SDN_ZONE",
      "renamed_to": [
        "sdn_zone"
      ],
      "type": "string",
      "required": true,
      "default_where": "none",
      "description": "Name of the SDN zone that holds every vnet of the list below. Created if absent. Its type is pinned to `simple` by the role (host-local, no VLAN, no VXLAN), so it is not a parameter here. The zone name is also the prefix Proxmox uses to build each subnet id, `<zone>-<network>-<mask>`."
    },
    {
      "name": "BUNDLE_SDN_VNETS",
      "type": "list",
      "items": "{ vnet: string, subnet: string (CIDR), gateway: string (optional), snat: bool (optional, default true) }",
      "required": true,
      "default_where": "none",
      "description": "Networks to create or reconcile. Each entry supplies its VNet and canonical subnet CIDR; snat defaults to true. Omitted gateway preserves an existing value or leaves a new subnet without a gateway. Explicit SNAT or gateway drift is updated, and missing enabled rules on applicable zone members can require one apply. A single-entry list is supported."
    },
    {
      "name": "proxmox_node",
      "type": "string",
      "required": true,
      "from_vault": true,
      "default_where": "none",
      "description": "API coordinator resolved from the scenario vault. The inventory must provide complete node-to-SSH coverage in proxmox_cli; reconciliation covers each subnet's zone members and preservation covers every cluster node."
    }
  ]
}
