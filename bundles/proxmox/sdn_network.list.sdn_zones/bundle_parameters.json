{
  "_generated": "DO NOT EDIT - generated from bundle_parameters.src.yml by _tools/generate-bundle-params.py",
  "tier": "proxmox",
  "bundle": "proxmox/sdn_network.list.sdn_zones",
  "description": "Read the SDN zones of the cluster and leave them in the fact `network_list_sdn_zones`, one dict per zone. Cluster-level read, no write, no apply.",
  "optional": false,
  "params": [
    {
      "name": "proxmox_node",
      "type": "string",
      "required": true,
      "from_vault": true,
      "default_where": "none",
      "description": "Proxmox node the read goes through, resolved from the scenario vault. It does not scope the result - the endpoint is cluster-level and returns every zone of the cluster."
    }
  ]
}
