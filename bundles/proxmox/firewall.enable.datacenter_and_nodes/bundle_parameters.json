{
  "_generated": "DO NOT EDIT - generated from bundle_parameters.src.yml by _tools/generate-bundle-params.py",
  "tier": "proxmox",
  "bundle": "proxmox/firewall.enable.datacenter_and_nodes",
  "description": "Arm the host firewall - the datacenter switch and the node switch - after guaranteeing management access on 8006 and 22 at both levels. Accepts first, then the datacenter, then the node : an accept can never cut anything, and both guards refuse to arm a level whose management ports are not reachable. Reads both switches back and fails if either stayed off. Does NOT filter any guest.",
  "optional": false,
  "params": [
    {
      "name": "proxmox_node",
      "type": "string",
      "required": true,
      "from_vault": true,
      "default_where": "none",
      "description": "Proxmox node whose firewall is armed, resolved from the scenario vault. Both endpoints are node-scoped or cluster-scoped through it."
    }
  ]
}
