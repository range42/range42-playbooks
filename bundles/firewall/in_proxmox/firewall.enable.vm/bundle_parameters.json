{
  "_generated": "DO NOT EDIT - generated from bundle_parameters.src.yml by _tools/generate-bundle-params.sh",
  "tier": "firewall",
  "bundle": "firewall/in_proxmox/firewall.enable.vm",
  "description": "Arm one guest firewall : the ssh accept on its chain, the firewall flag on its card or on all of them, then the guest switch. Accept first because the switch refuses a guest whose ssh is not reachable, and the flag before the switch so the last step is the one that turns filtering on. Reads the switch and the cards back and fails if either half did not take. Does NOT touch the datacenter or the node, and nothing filters until the datacenter switch is on.",
  "optional": false,
  "params": [
    {
      "name": "BUNDLE_VM_ID",
      "renamed_to": [
        "vm_id"
      ],
      "type": "int",
      "required": true,
      "default_where": "none",
      "description": "Proxmox VM id whose firewall is armed."
    },
    {
      "name": "BUNDLE_VM_VMNET_ID",
      "renamed_to": [
        "vm_vmnet_id"
      ],
      "type": "int",
      "required": false,
      "default_where": "call-site",
      "description": "Which card to touch, the N of netN. Left empty, EVERY card of the guest is touched - a guest reachable through a second card that stayed unflagged is a guest that is not isolated. Name one to leave the others alone, which is how a guest ends up filtered on one card and spoofable on another. A named card that does not exist is refused before anything is written."
    },
    {
      "name": "proxmox_node",
      "type": "string",
      "required": true,
      "from_vault": true,
      "default_where": "none",
      "description": "Proxmox node the guest runs on, resolved from the scenario vault. These endpoints are node-scoped."
    }
  ]
}
