{
  "_generated": "DO NOT EDIT - generated from bundle_parameters.src.yml by _tools/generate-bundle-params.sh",
  "tier": "firewall",
  "bundle": "firewall/in_proxmox/firewall.baseline.ssh_all_vms",
  "description": "Declare the ssh accept on every deployed guest of the active scenario - manifest intersected with what the node really runs, absentees skipped and named. Adds readiness, not safety : the arming family protects ssh by itself and refuses to arm without it. Idempotent per guest, inert until armed, and must run after the vm bootstrap stages since the guest firewall api refuses a vmid that does not run.",
  "optional": false,
  "params": [
    {
      "name": "proxmox_node",
      "type": "string",
      "required": true,
      "from_vault": true,
      "default_where": "none",
      "description": "Proxmox node the guests run on, resolved from the scenario vault. These endpoints are node-scoped."
    }
  ]
}
