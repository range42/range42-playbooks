{
  "_generated": "DO NOT EDIT - generated from bundle_parameters.src.yml by _tools/generate-bundle-params.sh",
  "tier": "firewall",
  "bundle": "firewall/in_proxmox/firewall.disable.vms",
  "description": "Stop filtering every guest of the ACTIVE scenario : all the switches off, then all the card flags off, and the ssh accepts POSTED rather than deleted so any later arming stays safe. The list comes from the scenario manifest, templates excluded. This hands MAC spoofing back on every card of the scenario at once, which is either what a MITM exercise wants or something to know before running it.",
  "optional": false,
  "params": [
    {
      "name": "proxmox_node",
      "type": "string",
      "required": true,
      "from_vault": true,
      "default_where": "none",
      "description": "Proxmox node the guest runs on, resolved from the scenario vault. These endpoints are node-scoped."
    }
  ]
}
