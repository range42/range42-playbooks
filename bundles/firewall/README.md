# firewall bundles : the Proxmox firewall and the firewall inside the VM

Two different firewalls, two sub-directories.

`in_proxmox/` is the **Proxmox firewall**, on the hypervisor : the `baseline.*` bundles declare rules (anti-lockout accepts on the datacenter and the node, an ssh accept on every VM, the open ports of each admin profile) without switching anything on ; the `enable.*` and `disable.*` bundles arm and disarm the datacenter, the node, one guest or every guest of the active scenario, always with the ssh accept posted first ; `report.status` reads the three levels. Arming is opt-in at deploy (`-e FIREWALL_ARM_VMS=YES`) or later through `range42-context networks-firewall-on`.

`in_vm/` is the firewall **inside the VM** (ufw), applied on an inventory group : the same five profiles also exist as `generic/network.baseline.*`, their former home, and both are imported by the scenarios today.

26 bundles. Every bundle is a playbook, `main.yml`, imported at parse time through `RANGE42_BUNDLE_DIR` ; its caller-facing parameters are declared in `bundle_parameters.src.yml` and published as `bundle_parameters.json` (see [../README.md](../README.md)).

## in_proxmox : the Proxmox firewall

| Bundle | What it does | Parameters |
|---|---|---|
| [`in_proxmox/firewall.baseline.deployer_backend_api`](in_proxmox/firewall.baseline.deployer_backend_api/) | Declare this profile's open ports (22, 8000) on one guest at the hypervisor level : one inbound ACCEPT per port on the guest's own chain, posted only when nothing already grants it, verified by read-back. | 2, [json](in_proxmox/firewall.baseline.deployer_backend_api/bundle_parameters.json), [src](in_proxmox/firewall.baseline.deployer_backend_api/bundle_parameters.src.yml) |
| [`in_proxmox/firewall.baseline.deployer_ui`](in_proxmox/firewall.baseline.deployer_ui/) | Declare this profile's open ports (22, 3000) on one guest at the hypervisor level : one inbound ACCEPT per port on the guest's own chain, posted only when nothing already grants it, verified by read-back. | 2, [json](in_proxmox/firewall.baseline.deployer_ui/bundle_parameters.json), [src](in_proxmox/firewall.baseline.deployer_ui/bundle_parameters.src.yml) |
| [`in_proxmox/firewall.baseline.gitea`](in_proxmox/firewall.baseline.gitea/) | Declare this profile's open ports (22, 2222, 3000) on one guest at the hypervisor level : one inbound ACCEPT per port on the guest's own chain, posted only when nothing already grants it, verified by read-back. | 2, [json](in_proxmox/firewall.baseline.gitea/bundle_parameters.json), [src](in_proxmox/firewall.baseline.gitea/bundle_parameters.src.yml) |
| [`in_proxmox/firewall.baseline.kong`](in_proxmox/firewall.baseline.kong/) | Declare this profile's open ports (22, 8000, 8443) on one guest at the hypervisor level : one inbound ACCEPT per port on the guest's own chain, posted only when nothing already grants it, verified by read-back. | 2, [json](in_proxmox/firewall.baseline.kong/bundle_parameters.json), [src](in_proxmox/firewall.baseline.kong/bundle_parameters.src.yml) |
| [`in_proxmox/firewall.baseline.management_access`](in_proxmox/firewall.baseline.management_access/) | Post the ssh (22) and api (8006) accepts on the datacenter chain and on the node chain - rules only, never a switch, so this bundle cannot arm anything. | 1, [json](in_proxmox/firewall.baseline.management_access/bundle_parameters.json), [src](in_proxmox/firewall.baseline.management_access/bundle_parameters.src.yml) |
| [`in_proxmox/firewall.baseline.mattermost`](in_proxmox/firewall.baseline.mattermost/) | Declare this profile's open ports (22, 8065) on one guest at the hypervisor level : one inbound ACCEPT per port on the guest's own chain, posted only when nothing already grants it, verified by read-back. | 2, [json](in_proxmox/firewall.baseline.mattermost/bundle_parameters.json), [src](in_proxmox/firewall.baseline.mattermost/bundle_parameters.src.yml) |
| [`in_proxmox/firewall.baseline.misp`](in_proxmox/firewall.baseline.misp/) | Declare this profile's open ports (22, 80, 443) on one guest at the hypervisor level : one inbound ACCEPT per port on the guest's own chain, posted only when nothing already grants it, verified by read-back. | 2, [json](in_proxmox/firewall.baseline.misp/bundle_parameters.json), [src](in_proxmox/firewall.baseline.misp/bundle_parameters.src.yml) |
| [`in_proxmox/firewall.baseline.nextcloud`](in_proxmox/firewall.baseline.nextcloud/) | Declare this profile's open ports (22, 8443) on one guest at the hypervisor level : one inbound ACCEPT per port on the guest's own chain, posted only when nothing already grants it, verified by read-back. | 2, [json](in_proxmox/firewall.baseline.nextcloud/bundle_parameters.json), [src](in_proxmox/firewall.baseline.nextcloud/bundle_parameters.src.yml) |
| [`in_proxmox/firewall.baseline.rocketchat`](in_proxmox/firewall.baseline.rocketchat/) | Declare this profile's open ports (22, 3500) on one guest at the hypervisor level : one inbound ACCEPT per port on the guest's own chain, posted only when nothing already grants it, verified by read-back. | 2, [json](in_proxmox/firewall.baseline.rocketchat/bundle_parameters.json), [src](in_proxmox/firewall.baseline.rocketchat/bundle_parameters.src.yml) |
| [`in_proxmox/firewall.baseline.ssh`](in_proxmox/firewall.baseline.ssh/) | Declare this profile's open ports (22) on one guest at the hypervisor level : one inbound ACCEPT per port on the guest's own chain, posted only when nothing already grants it, verified by read-back. | 2, [json](in_proxmox/firewall.baseline.ssh/bundle_parameters.json), [src](in_proxmox/firewall.baseline.ssh/bundle_parameters.src.yml) |
| [`in_proxmox/firewall.baseline.ssh_all_vms`](in_proxmox/firewall.baseline.ssh_all_vms/) | Declare the ssh accept on every deployed guest of the active scenario - manifest intersected with what the node really runs, absentees skipped and named. | 2, [json](in_proxmox/firewall.baseline.ssh_all_vms/bundle_parameters.json), [src](in_proxmox/firewall.baseline.ssh_all_vms/bundle_parameters.src.yml) |
| [`in_proxmox/firewall.baseline.ssh_http`](in_proxmox/firewall.baseline.ssh_http/) | Declare this profile's open ports (22, 80, 443) on one guest at the hypervisor level : one inbound ACCEPT per port on the guest's own chain, posted only when nothing already grants it, verified by read-back. | 2, [json](in_proxmox/firewall.baseline.ssh_http/bundle_parameters.json), [src](in_proxmox/firewall.baseline.ssh_http/bundle_parameters.src.yml) |
| [`in_proxmox/firewall.baseline.wazuh`](in_proxmox/firewall.baseline.wazuh/) | Declare this profile's open ports (22, 443, 1514, 1515, 55000) on one guest at the hypervisor level : one inbound ACCEPT per port on the guest's own chain, posted only when nothing already grants it, verified by read-back. | 2, [json](in_proxmox/firewall.baseline.wazuh/bundle_parameters.json), [src](in_proxmox/firewall.baseline.wazuh/bundle_parameters.src.yml) |
| [`in_proxmox/firewall.baseline.wazuh_server_agents_access`](in_proxmox/firewall.baseline.wazuh_server_agents_access/) | Let the agents of the scenario reach their wazuh manager : two inbound accepts on the SERVER guest, 1514 (events) and 55000 (api, enrollment), source-restricted to the scenario's vmnets. | 3, [json](in_proxmox/firewall.baseline.wazuh_server_agents_access/bundle_parameters.json), [src](in_proxmox/firewall.baseline.wazuh_server_agents_access/bundle_parameters.src.yml) |
| [`in_proxmox/firewall.disable.datacenter_and_nodes`](in_proxmox/firewall.disable.datacenter_and_nodes/README.md) | Turn the host firewall off - the datacenter switch first, then the node. | 1, [json](in_proxmox/firewall.disable.datacenter_and_nodes/bundle_parameters.json), [src](in_proxmox/firewall.disable.datacenter_and_nodes/bundle_parameters.src.yml) |
| [`in_proxmox/firewall.disable.vm`](in_proxmox/firewall.disable.vm/README.md) | Stop filtering one guest : the guest switch off, then the firewall flag off on its card or on all of them. | 3, [json](in_proxmox/firewall.disable.vm/bundle_parameters.json), [src](in_proxmox/firewall.disable.vm/bundle_parameters.src.yml) |
| [`in_proxmox/firewall.disable.vms`](in_proxmox/firewall.disable.vms/README.md) | Stop filtering every guest of the ACTIVE scenario : all the switches off, then all the card flags off, and the ssh accepts POSTED rather than deleted so any later arming stays safe. | 1, [json](in_proxmox/firewall.disable.vms/bundle_parameters.json), [src](in_proxmox/firewall.disable.vms/bundle_parameters.src.yml) |
| [`in_proxmox/firewall.enable.datacenter_and_nodes`](in_proxmox/firewall.enable.datacenter_and_nodes/README.md) | Arm the host firewall - the datacenter switch and the node switch - after guaranteeing management access on 8006 and 22 at both levels. | 1, [json](in_proxmox/firewall.enable.datacenter_and_nodes/bundle_parameters.json), [src](in_proxmox/firewall.enable.datacenter_and_nodes/bundle_parameters.src.yml) |
| [`in_proxmox/firewall.enable.vm`](in_proxmox/firewall.enable.vm/README.md) | Arm one guest firewall : the ssh accept on its chain, the firewall flag on its card or on all of them, then the guest switch. | 3, [json](in_proxmox/firewall.enable.vm/bundle_parameters.json), [src](in_proxmox/firewall.enable.vm/bundle_parameters.src.yml) |
| [`in_proxmox/firewall.enable.vms`](in_proxmox/firewall.enable.vms/README.md) | Arm every guest of the ACTIVE scenario, card flags included, in the same order as the single-guest bundle : all the ssh accepts, then all the card flags, then all the switches. | 1, [json](in_proxmox/firewall.enable.vms/bundle_parameters.json), [src](in_proxmox/firewall.enable.vms/bundle_parameters.src.yml) |
| [`in_proxmox/firewall.report.status`](in_proxmox/firewall.report.status/) | Read and print the arming state of the three levels - datacenter switch, node switch, and for every deployed guest of the active scenario its own switch and its per-card flags. | 1, [json](in_proxmox/firewall.report.status/bundle_parameters.json), [src](in_proxmox/firewall.report.status/bundle_parameters.src.yml) |

## in_vm : the firewall inside the VM (ufw)

| Bundle | What it does | Parameters |
|---|---|---|
| [`in_vm/os_firewall.baseline.deployer_backend_api`](in_vm/os_firewall.baseline.deployer_backend_api/) | Firewall baseline opening SSH (22) and the deployer-backend-api FastAPI port (8000) on the target group. | 1, [json](in_vm/os_firewall.baseline.deployer_backend_api/bundle_parameters.json), [src](in_vm/os_firewall.baseline.deployer_backend_api/bundle_parameters.src.yml) |
| [`in_vm/os_firewall.baseline.deployer_ui`](in_vm/os_firewall.baseline.deployer_ui/) | Firewall baseline opening SSH (22) and the range42-deployer-ui Vite/Node port (3000) on the target group. | 1, [json](in_vm/os_firewall.baseline.deployer_ui/bundle_parameters.json), [src](in_vm/os_firewall.baseline.deployer_ui/bundle_parameters.src.yml) |
| [`in_vm/os_firewall.baseline.kong`](in_vm/os_firewall.baseline.kong/) | Network baseline policy that opens 22/tcp plus Kong proxy ports 8000/tcp and 8443/tcp on the target group | 1, [json](in_vm/os_firewall.baseline.kong/bundle_parameters.json), [src](in_vm/os_firewall.baseline.kong/bundle_parameters.src.yml) |
| [`in_vm/os_firewall.baseline.ssh`](in_vm/os_firewall.baseline.ssh/) | Network baseline policy that opens 22/tcp on the target group (SSH-only minimum for any managed VM) | 1, [json](in_vm/os_firewall.baseline.ssh/bundle_parameters.json), [src](in_vm/os_firewall.baseline.ssh/bundle_parameters.src.yml) |
| [`in_vm/os_firewall.baseline.ssh_http`](in_vm/os_firewall.baseline.ssh_http/) | Open the standard web-facing firewall profile (ports 22, 80, 443 tcp) on a target group. | 1, [json](in_vm/os_firewall.baseline.ssh_http/bundle_parameters.json), [src](in_vm/os_firewall.baseline.ssh_http/bundle_parameters.src.yml) |

Regenerate this index with `_tools/generate-bundle-index.py` after adding or re-describing a bundle.
