{
  "_generated": "DO NOT EDIT - generated from bundle_parameters.src.yml by _tools/generate-bundle-params.py",
  "tier": "ctf",
  "bundle": "ctf/misconfiguration/network/vsftpd/ftp_anon_server",
  "description": "Deploy the vsftpd anonymous FTP misconfiguration vulnerable container via docker-compose for CTF training",
  "optional": false,
  "params": [
    {
      "name": "global_vm_ssh_name",
      "target": true,
      "type": "string",
      "required": true,
      "default_where": "none",
      "description": "Inventory hostname of the target VM that will run the vulnerable container"
    },
    {
      "name": "OPERATOR_USER",
      "type": "string",
      "required": false,
      "default_where": "bundle-inline",
      "default": "{{ default_admin_vm_ci_user }}",
      "description": "SSH user owning the remote deploy dir, defaults to default_admin_vm_ci_user from group_vars"
    },
    {
      "name": "REMOTE_PROJECT_DIR",
      "type": "string",
      "required": false,
      "default_where": "bundle-inline",
      "default": "/tmp/deploy-ftp_anon_server",
      "description": "Remote path where the compose project is copied and brought up"
    },
    {
      "name": "CLEAN_UP_DEPLOY_DIR",
      "type": "bool",
      "required": false,
      "default_where": "bundle-inline",
      "default": "NO",
      "bool_style": "yesno",
      "allowed": [
        "YES",
        "NO"
      ],
      "description": "When YES the remote deploy dir is removed after the compose stack is up"
    },
    {
      "name": "SEND_POC_DIR",
      "type": "bool",
      "required": false,
      "default_where": "bundle-inline",
      "default": "NO",
      "bool_style": "yesno",
      "allowed": [
        "YES",
        "NO"
      ],
      "description": "When YES the proof-of-concept files are shipped alongside the compose project"
    }
  ]
}
