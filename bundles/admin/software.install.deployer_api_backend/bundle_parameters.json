{
  "_generated": "DO NOT EDIT - generated from bundle_parameters.src.yml by _tools/generate-bundle-params.py",
  "tier": "admin",
  "bundle": "admin/software.install.deployer_api_backend",
  "description": "Manage one authenticated immutable backend container with persistent credentials, verified unchanged repeat, guarded managed updates and explicit systemd adoption through a reviewed private proof file.",
  "optional": true,
  "install_flag": "INSTALL_DEPLOYER_API_BACKEND",
  "params": [
    {
      "name": "global_vm_ssh_name",
      "type": "string",
      "required": true,
      "default_where": "none",
      "description": "Inventory host on which the local Docker daemon and managed paths exist.",
      "target": true
    },
    {
      "name": "BACKEND_IMAGE",
      "type": "string",
      "required": true,
      "default_where": "none",
      "description": "Already available immutable image ID sha256:<digest> or repository@sha256:<digest>; never builds or pulls implicitly."
    },
    {
      "name": "BACKEND_INSTALL_ROOT",
      "type": "string",
      "required": false,
      "default_where": "bundle-inline",
      "description": "Private managed installation record, lock and immutable releases root. Existing unknown data is refused.",
      "default": "/var/lib/range42-backend"
    },
    {
      "name": "BACKEND_INSTALL_NAME",
      "type": "string",
      "required": false,
      "default_where": "bundle-inline",
      "description": "Short Docker installation identifier.",
      "default": "range42-backend"
    },
    {
      "name": "BACKEND_INSTALL_ACTION",
      "type": "string",
      "required": false,
      "default_where": "bundle-inline",
      "description": "apply installs fresh or verifies unchanged; fresh refuses existing; update requests a guarded managed cutover; adopt-systemd requires a reviewed private target-host adoption file.",
      "default": "apply",
      "allowed": [
        "apply",
        "fresh",
        "update",
        "adopt-systemd"
      ]
    },
    {
      "name": "BACKEND_SYSTEMD_ADOPTION_FILE",
      "type": "string",
      "required": false,
      "default_where": "none",
      "description": "Absolute private target-host migration proof path, required only for adopt-systemd. Contents are validated by the consumer and never included in public Ansible output. Other actions must omit this parameter."
    },
    {
      "name": "BACKEND_UID",
      "type": "int",
      "required": false,
      "default_where": "bundle-inline",
      "description": "Host/container UID sharing persistent file locks.",
      "default": 1000
    },
    {
      "name": "BACKEND_GID",
      "type": "int",
      "required": false,
      "default_where": "bundle-inline",
      "description": "Host/container GID for persistent state and private credentials.",
      "default": 1000
    },
    {
      "name": "API_PORT",
      "type": "int",
      "required": false,
      "default_where": "bundle-inline",
      "description": "Published listener port in bridge mode or explicit API port in host mode; does not install or configure firewall services.",
      "default": 8000
    },
    {
      "name": "BACKEND_LISTEN_ADDRESS",
      "type": "string",
      "required": false,
      "default_where": "bundle-inline",
      "description": "Explicit bind address; loopback by default.",
      "default": "127.0.0.1"
    },
    {
      "name": "BACKEND_STATE_DIR",
      "type": "string",
      "required": false,
      "default_where": "none",
      "description": "Persistent state host directory; defaults to <root>/state."
    },
    {
      "name": "BACKEND_WORKSPACE_HOST",
      "type": "string",
      "required": false,
      "default_where": "none",
      "description": "Persistent host workspace directory; defaults to <state>/workspaces."
    },
    {
      "name": "BACKEND_WORKSPACE_CONTAINER",
      "type": "string",
      "required": false,
      "default_where": "none",
      "description": "Exact workspace path inside the image, including an explicit legacy /home path if needed."
    },
    {
      "name": "BACKEND_DATABASE_CONTAINER",
      "type": "string",
      "required": false,
      "default_where": "none",
      "description": "Exact SQLite path inside the configured workspace or persistent /var/lib/range42 state mount; defaults to <workspace>/.range42.db. Preserve the original absolute path when adopting existing state."
    },
    {
      "name": "BACKEND_SECRETS_DIR",
      "type": "string",
      "required": false,
      "default_where": "none",
      "description": "Private original api-token and credential-key files; defaults to <root>/secrets."
    },
    {
      "name": "BACKEND_RUNTIME_DIR",
      "type": "string",
      "required": false,
      "default_where": "none",
      "description": "Complete reviewed runtime export on target host; copied into a private immutable release. Profile, Ansible configuration and CA must match the explicitly configured container paths."
    },
    {
      "name": "BACKEND_RUNTIME_CONTAINER",
      "type": "string",
      "required": false,
      "default_where": "none",
      "description": "Absolute container runtime directory; planner default is /runtime. Preserve the installed path for an existing runtime profile."
    },
    {
      "name": "BACKEND_RUNTIME_CONFIG_CONTAINER",
      "type": "string",
      "required": false,
      "default_where": "none",
      "description": "Absolute container DIRECTORY for the read-only ansible.cfg and bundle-runtime.json files; defaults to the runtime container directory."
    },
    {
      "name": "BACKEND_RUNTIME_CA_CONTAINER",
      "type": "string",
      "required": false,
      "default_where": "none",
      "description": "Absolute container FILE path for the reviewed combined CA; defaults to <runtime container>/proxmox-ca.pem."
    },
    {
      "name": "BACKEND_WORKSPACE_TEMPLATE_DIR",
      "type": "string",
      "required": false,
      "default_where": "none",
      "description": "Dedicated private workspace credential template on target host, copied with runtime; never the operator SSH home."
    },
    {
      "name": "BACKEND_WORKSPACE_TEMPLATE_CONTAINER",
      "type": "string",
      "required": false,
      "default_where": "none",
      "description": "Absolute container directory for the read-only private workspace template; planner default is /run/range42-template."
    },
    {
      "name": "BACKEND_NETWORK_MODE",
      "type": "string",
      "required": false,
      "default_where": "none",
      "description": "bridge uses Docker bridge networking and a published port (default). host shares the host network and starts the API on the explicit address/port without port publication. Requires separately reviewed reachability.",
      "allowed": [
        "bridge",
        "host"
      ]
    },
    {
      "name": "BACKEND_INVENTORY_CONTAINER",
      "type": "string",
      "required": false,
      "default_where": "none",
      "description": "Optional absolute inventory directory inside the existing state or workspace mount. Sets API_BACKEND_INVENTORY_DIR without adding a mount; omitted by default."
    },
    {
      "name": "BACKEND_VAULT_PASSWORD_HOST",
      "type": "string",
      "required": false,
      "default_where": "none",
      "description": "Optional existing target-host vault password file, paired with BACKEND_VAULT_PASSWORD_CONTAINER. Empty by default. The installer preserves its private bytes and never generates or rotates this secret."
    },
    {
      "name": "BACKEND_VAULT_PASSWORD_CONTAINER",
      "type": "string",
      "required": false,
      "default_where": "none",
      "description": "Optional absolute container file path for the existing read-only vault password binding; paired with BACKEND_VAULT_PASSWORD_HOST and empty by default. Sets VAULT_PASSWORD_FILE without exposing the password in variables."
    },
    {
      "name": "BACKEND_CORS_ORIGINS",
      "type": "list",
      "items": "exact HTTP(S) origin strings",
      "required": false,
      "default_where": "none",
      "description": "List of exact allowed HTTP(S) browser origins; defaults to an empty list."
    },
    {
      "name": "BACKEND_GIT_ALLOWED_HOSTS",
      "type": "list",
      "items": "DNS host strings with optional ports",
      "required": false,
      "default_where": "none",
      "description": "List of allowed Git hosts; defaults to github.com, gitlab.com, codeberg.org."
    },
    {
      "name": "REMOTE_PROJECT_DIR",
      "type": "string",
      "required": false,
      "default_where": "none",
      "description": "Legacy alias for BACKEND_INSTALL_ROOT only; does not imply adopting existing data."
    },
    {
      "name": "WORKSPACE_DIR_HOST",
      "type": "string",
      "required": false,
      "default_where": "none",
      "description": "Legacy alias for BACKEND_WORKSPACE_HOST only; historical data requires explicit offline adoption review."
    }
  ]
}
