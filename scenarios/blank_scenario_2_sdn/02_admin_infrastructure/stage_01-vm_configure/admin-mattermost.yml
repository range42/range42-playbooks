##
## admin-mattermost - Mattermost docker-compose stack on r42.bs2-admin-mattermost
##
## Delegates to the shared bundle (catalog .env seed + Docker baseline +
## firewall 22/8065 + docker-compose deploy of mattermost : postgres +
## mattermost + provisioner). blank_scenario_2_sdn offers Mattermost as an
## optional admin service (the mattermost_lab vitrine is the dedicated showcase).
##
## Gated by INSTALL_MATTERMOST (default NO per blank_scenario_2_sdn feature_flags.yml).
##

- import_playbook: "{{ lookup('env', 'RANGE42_BUNDLE_DIR') }}/admin/software.install.mattermost/main.yml"
  when: INSTALL_MATTERMOST | default("NO") | upper == "YES"
  vars:
    global_vm_ssh_name: "r42.bs2-admin-mattermost"
    global_vm_ci_ip:    "192.168.142.126"

## The HYPERVISOR-side declaration of the same ports, on the tap - out of reach of a local
## root. Idempotent, and inert until the guest is ARMED (firewall.enable.*).
- import_playbook: "{{ lookup('env', 'RANGE42_BUNDLE_DIR') }}/firewall/in_proxmox/firewall.baseline.mattermost/main.yml"
  when: INSTALL_MATTERMOST | default("NO") | upper == "YES"
  vars:
    BUNDLE_VM_ID: 2126
