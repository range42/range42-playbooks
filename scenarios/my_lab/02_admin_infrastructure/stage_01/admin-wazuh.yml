##
## stage_01 — software install for admin-wazuh
## catalog roles are referenced BY NAME (resolved via ANSIBLE_ROLES_PATH at deploy).
##

- name: "configure admin-wazuh"
  hosts: r42.admin-wazuh
  become: true
  vars_files:
    - "../../secrets/default_vault.yml"
  roles:
    - software.configure.firewalls
    - software.install.wazuh
