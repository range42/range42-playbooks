##
##


#### #### #### #### #### #### #### #### #### #### #### #### #### #### #### #### #### #### #### #### #### #### ####
#
# PROXMOX INIT - download cloud init files (nextcloud_lab : ubuntu noble only)
#
# nextcloud_lab uses only the ubuntu noble minimal cloud image (for template 9232).
# Trimmed from demo_lab's download list (which pulls all 3 OS images).
#
#### #### #### #### #### #### #### #### #### #### #### #### #### #### #### #### #### #### #### #### #### #### ####

- hosts: proxmox
  gather_facts: false
  vars_files:
    - "../../secrets/default_vault.yml"

  tasks:
    - name: PROXMOX INIT - DOWNLOAD - CLOUD INIT image (ubuntu noble minimal)
      include_role:
        name: range42-ansible_roles-proxmox_controller

      vars:
        proxmox_vm_action: "storage_download_iso"
        proxmox_storage: "local"
        iso_file_content_type: "iso"
        iso_url: "{{ item.iso_url }}"
        iso_file_name: "{{ item.iso_file_name }}"

      loop:
        - {
            iso_url: "https://cloud-images.ubuntu.com/minimal/daily/noble/current/noble-minimal-cloudimg-amd64.img",
            iso_file_name: "noble-minimal-cloudimg-amd64.img",
          }
