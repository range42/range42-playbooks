##
## admin-misp - MISP threat intel stack on r42.bs6-admin-misp
##
## Delegates to the shared bundle (Docker install + docker-compose deploy of
## misp-standalone). Gated by INSTALL_MISP (default NO).
##

- import_playbook: "{{ lookup('env', 'RANGE42_GITDIR__ROOT_DIR') }}/range42-playbooks/bundles/admin/software.install.misp_standalone/main.yml"
  when: INSTALL_MISP | default("NO") | upper == "YES"
  vars:
    global_vm_ssh_name: "r42.bs6-admin-misp"
    global_vm_ci_ip:    "192.168.142.144"
