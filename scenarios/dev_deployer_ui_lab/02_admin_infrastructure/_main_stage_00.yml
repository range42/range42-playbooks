---
##
## 02 - admin infrastructure - stage_00 (vm_bootstrap)
##
## Structure : stage_00 unit imported by top-level main.yml ; split kept so global stage ordering (all stage_00 before all stage_01) stays editable.
##
## Per-VM bootstrap of the admin platform VMs (admin-wazuh gated by INSTALL_WAZUH,
## admin-misp gated by INSTALL_MISP - both default NO). With every flag NO, no
## admin VM is created.
##
## Imported from the top-level main.yml during the global stage_00 phase
## (ALL tiers stage_00 run before ANY tier stage_01). See main.yml.
##

- import_playbook: ./stage_00-vm_bootstrap/_r42_admin_group.yml
