##
## debug_scenario_b - create template 9232 (template-vm-medium-02-8g-64g).
##
## Required for the optional admin tier (admin-wazuh / admin-misp gated by
## INSTALL_WAZUH / INSTALL_MISP, default NO). Idempotent - skipped if the
## template already exists (e.g. built by another scenario on the same
## Proxmox).
##

- import_playbook: ./03-template-vm-medium-02-8g-64g.yml
