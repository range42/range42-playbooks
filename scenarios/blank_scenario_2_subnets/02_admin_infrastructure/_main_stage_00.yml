---
##
## 02 - admin infrastructure - stage_00 (vm_bootstrap)
##
## Structure : stage_00 unit imported by top-level main.yml ; split kept so global stage ordering (all stage_00 before all stage_01) stays editable.
##
## Per-VM bootstrap of the admin platform VMs (9 slots, EVERY one gated by its INSTALL_<flag>) :
##   - the deployer trio (api-gateway / api-backend / ui), gated INSTALL_DEPLOYER_UI=YES
##   - admin-wazuh, admin-misp, admin-gitea, admin-mattermost, admin-nextcloud,
##     admin-rocketchat, each gated by its own flag
##
## Per-VM gating handled inside stage_00-vm_bootstrap/_r42_admin_group.yml.
##

- import_playbook: ./stage_00-vm_bootstrap/_r42_admin_group.yml
