"""Shared test fixtures for r42topo.

The canonical-engine tests load the shared schema test-vectors directly from
``tests/vectors/test-vectors/`` (see each test module), so no topology fixture
is needed here. This file is intentionally minimal after the invented-model
retirement (issue #67).
"""
