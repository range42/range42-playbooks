#
# debian trixie (13) templates — idempotent create + apt-proxy + update-to-latest
#

- import_playbook: ./00-template-vm-debian-small.yml
- import_playbook: ./01-template-vm-debian-medium.yml

# apt proxy cicustom (no-op if apt_proxy_url empty)
- import_playbook: ./_apply_apt_proxy.yml
# boot, apt update + dist-upgrade via cloud-init, then convert to template
- import_playbook: ./_update_templates.yml
